-- ==========================================================
-- OBFUSCATED CLOUD LOADER ENGINE
-- ==========================================================
local player = game:GetService("Players").LocalPlayer
local userEnteredKey = tostring(_G.script_key or ""):gsub("%s", "")

local VALID_KEYS = {
    ["PANCAKE-NEXUS-2026"] = true,
    ["PANCAKE-GOLD-9982"]  = true,
    ["XENO-ADMIN-VOID"]    = true,
}

local function GetValidKeyList()
    local keys = {}
    for key in pairs(VALID_KEYS) do
        table.insert(keys, key)
    end
    return table.concat(keys, ", ")
end

local function ShowKeyError()
    local sg = Instance.new("ScreenGui")
    sg.Name = "PancakeKeyError"
    sg.ResetOnSpawn = false
    sg.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    sg.Parent = cloneref(game:GetService("CoreGui") or gethui())

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 430, 0, 200)
    frame.Position = UDim2.new(0.5, -215, 0.5, -100)
    frame.BackgroundColor3 = Color3.fromRGB(13, 14, 21)
    frame.BorderSizePixel = 0
    frame.Parent = sg
    local frameCorner = Instance.new("UICorner")
    frameCorner.CornerRadius = UDim.new(0, 10)
    frameCorner.Parent = frame
    local frameStroke = Instance.new("UIStroke")
    frameStroke.Color = Color3.fromRGB(120, 40, 40)
    frameStroke.Thickness = 2
    frameStroke.Parent = frame

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -50, 0, 40)
    title.Position = UDim2.new(0, 15, 0, 10)
    title.BackgroundTransparency = 1
    title.Text = "INVALID KEY"
    title.TextColor3 = Color3.fromRGB(255, 90, 90)
    title.Font = Enum.Font.GothamBold
    title.TextSize = 20
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = frame

    local msg = Instance.new("TextLabel")
    msg.Size = UDim2.new(1, -30, 0, 120)
    msg.Position = UDim2.new(0, 15, 0, 52)
    msg.BackgroundTransparency = 1
    msg.Text = 'Set _G.script_key = "<key>" BEFORE running this script.\n\nYou entered: ' .. tostring(userEnteredKey)
    msg.TextColor3 = Color3.fromRGB(220, 220, 230)
    msg.Font = Enum.Font.Gotham
    msg.TextSize = 14
    msg.TextXAlignment = Enum.TextXAlignment.Left
    msg.TextYAlignment = Enum.TextYAlignment.Top
    msg.TextWrapped = true
    msg.Parent = frame

    local close = Instance.new("TextButton")
    close.Size = UDim2.new(0, 30, 0, 30)
    close.Position = UDim2.new(1, -36, 0, 6)
    close.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    close.Text = "X"
    close.TextColor3 = Color3.fromRGB(255, 255, 255)
    close.Font = Enum.Font.GothamBold
    close.TextSize = 16
    close.Parent = frame
    local closeCorner = Instance.new("UICorner")
    closeCorner.CornerRadius = UDim.new(0, 8)
    closeCorner.Parent = close
    close.MouseButton1Click:Connect(function()
        sg:Destroy()
    end)

    task.delay(15, function()
        if sg and sg.Parent then
            sg:Destroy()
        end
    end)
end

if VALID_KEYS[userEnteredKey] then
    print("[SUCCESS]: Key validated. Running protected payload...")

    local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/Stefanuk12/LinoriaLib/main/Library.lua"))()

    local Players = game:GetService("Players")
    local RunService = game:GetService("RunService")
    local Workspace = game:GetService("Workspace")
    local LocalPlayer = Players.LocalPlayer
    local Camera = Workspace.CurrentCamera or Workspace:FindFirstChildOfClass("Camera")

    local Window = Library:CreateWindow({
        Title = "Pancake Hub",
        Center = true,
        AutoShow = false,
        TabPadding = 8,
        MenuFadeTime = 0.2,
    })

    local Tabs = {
        Aiming = Window:AddTab("Aiming"),
        ESP = Window:AddTab("ESP"),
        ["UI Settings"] = Window:AddTab("UI Settings"),
    }

    function Library.Toggle()
        Window.Holder.Visible = not Window.Holder.Visible
    end
    Window.Holder.Visible = true

    local AimingGroup = Tabs.Aiming:AddLeftGroupbox("Aimbot")

    local EnabledToggle = AimingGroup:AddToggle("Enabled", {
        Text = "Enabled",
        Default = true,
        Tooltip = "Master toggle for the aimbot",
    })
    EnabledToggle:AddKeyPicker("EnabledKey", {
        Text = "Aim Key",
        Default = Enum.UserInputType.MouseButton2,
        SyncToggleState = false,
        Mode = "Hold",
        Modes = {"Hold", "Toggle"},
        IgnoreProcessedCheck = true,
    })
    local EnabledKey = EnabledToggle.Addons[1]

    local FOVSlider = AimingGroup:AddSlider("FOV", {
        Text = "FOV",
        Default = 90,
        Min = 5,
        Max = 200,
        Suffix = "px",
        Rounding = 1,
        Tooltip = "Only targets inside this distance from the crosshair get locked",
    })

    local HitboxDropdown = AimingGroup:AddDropdown("Hitbox", {
        Text = "Hitbox",
        Values = {"Head", "HumanoidRootPart", "Torso"},
        Default = 1,
        Multi = false,
        Tooltip = "Which body part to aim at",
    })
    HitboxDropdown:OnChanged(function()
        HitboxDropdown:CloseDropdown()
    end)

    local SmoothnessSlider = AimingGroup:AddSlider("Smoothness", {
        Text = "Smoothness",
        Default = 60,
        Min = 0,
        Max = 100,
        Suffix = "%",
        Rounding = 1,
        Tooltip = "Higher = faster lock-on. 100% = instant snap",
    })

    local MaxDistanceSlider = AimingGroup:AddSlider("MaxDistance", {
        Text = "Max Distance",
        Default = 200,
        Min = 5,
        Max = 1500,
        Suffix = " studs",
        Rounding = 1,
        Tooltip = "Maximum range to look for targets",
    })

    local PredictionSlider = AimingGroup:AddSlider("Prediction", {
        Text = "Prediction",
        Default = 0,
        Min = 0,
        Max = 1,
        Rounding = 2,
        Tooltip = "Leads moving targets (0 = none)",
    })

    local ExtrasGroup = Tabs.Aiming:AddRightGroupbox("Extras")

    local TeamCheckToggle = ExtrasGroup:AddToggle("TeamCheck", {
        Text = "Ignore Teammates",
        Default = true,
        Tooltip = "Do not aim at players on your team",
    })

    local WallCheckToggle = ExtrasGroup:AddToggle("WallCheck", {
        Text = "Wall Check",
        Default = true,
        Tooltip = "Skip targets that are behind walls",
    })

    local PriorityDropdown = ExtrasGroup:AddDropdown("Priority", {
        Text = "Target Priority",
        Values = {"Closest to Crosshair", "Lowest HP", "Nearest Distance"},
        Default = 1,
        Multi = false,
        Tooltip = "How to pick the best target",
    })
    PriorityDropdown:OnChanged(function()
        PriorityDropdown:CloseDropdown()
    end)

    local SilentAimToggle = ExtrasGroup:AddToggle("SilentAim", {
        Text = "Silent Aim",
        Default = false,
        Tooltip = "Redirect bullets toward the target (hooks raycasts)",
    })

    local function GetCharacter(Player)
        return Player.Character
    end

    local function IsAlive(Character)
        if (not Character) then
            return false
        end
        local Humanoid = Character:FindFirstChildOfClass("Humanoid")
        local RootPart = Character:FindFirstChild("HumanoidRootPart")
        return Humanoid and RootPart and Humanoid.Health > 0
    end

    local function IsVisible(TargetPos)
        local Origin = Camera.CFrame.Position
        local Direction = (TargetPos - Origin)
        local Params = RaycastParams.new()
        Params.FilterType = Enum.RaycastFilterType.Exclude
        Params.FilterDescendantsInstances = {LocalPlayer.Character}
        local Result = Workspace:Raycast(Origin, Direction, Params)
        return Result == nil
    end

    local function IsTeammateOf(Player)
        return LocalPlayer.Team and Player.Team and Player.Team == LocalPlayer.Team
    end

    local function GetPlayersInFOV()
        local ViewportCenter = Camera.ViewportSize / 2
        local Fov = FOVSlider.Value
        local Candidates = {}

        for _, Player in ipairs(Players:GetPlayers()) do
            if (Player == LocalPlayer) then continue end
            if (TeamCheckToggle.Value and IsTeammateOf(Player)) then continue end

            local Character = GetCharacter(Player)
            if (not IsAlive(Character)) then continue end

            local RootPart = Character:FindFirstChild("HumanoidRootPart")
            if (not RootPart) then continue end

            local Range = (RootPart.Position - Camera.CFrame.Position).Magnitude
            if (Range > MaxDistanceSlider.Value) then continue end

            local Part = Character:FindFirstChild(HitboxDropdown.Value) or RootPart
            local ScreenPos, OnScreen = Camera:WorldToScreenPoint(Part.Position)
            if (not OnScreen) then continue end

            local ScreenDistance = (Vector2.new(ScreenPos.X, ScreenPos.Y) - ViewportCenter).Magnitude
            if (ScreenDistance > Fov) then continue end

            if (WallCheckToggle.Value and not IsVisible(Part.Position)) then continue end

            local Humanoid = Character:FindFirstChildOfClass("Humanoid")
            table.insert(Candidates, {
                Player = Player,
                Part = Part,
                ScreenDistance = ScreenDistance,
                Range = Range,
                Health = Humanoid and Humanoid.Health or math.huge,
            })
        end

        local Priority = PriorityDropdown.Value
        if (Priority == "Lowest HP") then
            table.sort(Candidates, function(a, b) return a.Health < b.Health end)
        elseif (Priority == "Nearest Distance") then
            table.sort(Candidates, function(a, b) return a.Range < b.Range end)
        else
            table.sort(Candidates, function(a, b) return a.ScreenDistance < b.ScreenDistance end)
        end

        return Candidates
    end

    local function GetSilentAimTarget()
        local Candidates = GetPlayersInFOV()
        if (#Candidates == 0) then return nil end
        local Best = Candidates[1]
        local Point = Best.Part.Position
        local Humanoid = Best.Player.Character and Best.Player.Character:FindFirstChildOfClass("Humanoid")
        if (PredictionSlider.Value > 0 and Humanoid) then
            local Velocity = Best.Part.AssemblyLinearVelocity or Vector3.new()
            local TravelTime = (Point - Camera.CFrame.Position).Magnitude / 1000
            Point = Point + Velocity * TravelTime * PredictionSlider.Value
        end
        return Point
    end

    local function GetTargetPoint()
        local Candidates = GetPlayersInFOV()
        if (#Candidates == 0) then return nil end
        local Best = Candidates[1]
        local Point = Best.Part.Position
        if (PredictionSlider.Value > 0) then
            local Velocity = Best.Part.AssemblyLinearVelocity or Vector3.new()
            local TravelTime = (Point - Camera.CFrame.Position).Magnitude / 1000
            Point = Point + Velocity * TravelTime * PredictionSlider.Value
        end
        return Point
    end
    RunService:UnbindFromRenderStep("BlockSpinLegitAim")
    RunService:BindToRenderStep("BlockSpinLegitAim", Enum.RenderPriority.Camera.Value + 1, function()
        local Active = EnabledToggle.Value and EnabledKey:GetState()
        if (not Active) then return end
        local Target = GetTargetPoint()
        if (not Target) then return end
        local Desired = CFrame.lookAt(Camera.CFrame.Position, Target)
        local Smooth = SmoothnessSlider.Value / 100
        if (Smooth >= 1) then
            Camera.CFrame = Desired
        else
            Camera.CFrame = Camera.CFrame:Lerp(Desired, Smooth)
        end
    end)

    local NamecallHook
    NamecallHook = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
        local Method = getnamecallmethod()
        local Args = {...}
        if (SilentAimToggle.Value) then
            local LocalChar = LocalPlayer.Character
            if (LocalChar and self == Workspace) then
                if (Method == "Raycast" or Method == "FindPartOnRay" or Method == "Blockcast") then
                    local Target = GetSilentAimTarget()
                    if (Target) then
                        local Origin = Camera.CFrame.Position
                        if (Method == "FindPartOnRay") then
                            Args[1] = Ray.new(Origin, (Target - Origin).Unit * 500)
                        else
                            Args[2] = (Target - Origin)
                        end
                        return NamecallHook(self, unpack(Args))
                    end
                end
            end
        end
        return NamecallHook(self, ...)
    end))

    local ESPTab = Tabs.ESP
    local ESPGroup = ESPTab:AddLeftGroupbox("ESP")

    local ESPToggle = ESPGroup:AddToggle("ESPEnabled", {
        Text = "Enabled", Default = true,
    })
    local ESPBoxesToggle = ESPGroup:AddToggle("ESPBoxes", {
        Text = "Boxes", Default = true,
    })
    local ESPBoxStyleDropdown = ESPGroup:AddDropdown("ESPBoxStyle", {
        Text = "Box Style",
        Values = {"2D Box", "Corner Box"},
        Default = 1, Multi = false,
    })
    ESPBoxStyleDropdown:OnChanged(function() ESPBoxStyleDropdown:CloseDropdown() end)

    local ESPNameToggle = ESPGroup:AddToggle("ESPName", {
        Text = "Names", Default = true,
    })
    local ESPHealthToggle = ESPGroup:AddToggle("ESPHealth", {
        Text = "Health Bar", Default = true,
    })
    local ESPHealthTextToggle = ESPGroup:AddToggle("ESPHealthText", {
        Text = "Health Text", Default = true,
    })
    local ESPTracerToggle = ESPGroup:AddToggle("ESPTracers", {
        Text = "Tracers", Default = false,
    })
    local ESPTeamCheckToggle = ESPGroup:AddToggle("ESPTeamCheck", {
        Text = "Ignore Teammates", Default = true,
    })
    local ESPDistanceSlider = ESPGroup:AddSlider("ESPDistance", {
        Text = "Max Distance", Default = 200, Min = 5, Max = 1500,
        Suffix = " studs", Rounding = 1,
    })
    local ESPColorPicker = ESPGroup:AddColorPicker("ESPColor", {
        Text = "ESP Color", Default = Color3.fromRGB(255, 60, 60),
    })
    local ESPChamsToggle = ESPGroup:AddToggle("ESPChams", {
        Text = "Chams", Default = false,
    })
    local ESPChamsColorPicker = ESPGroup:AddColorPicker("ESPChamsColor", {
        Text = "Chams Color", Default = Color3.fromRGB(255, 60, 60),
    })

    local ESPCache = {}
    local SmoothCache = {}
    local ChamCache = {}

    local function GetHealthColor(HealthPercent)
        if (HealthPercent > 0.5) then
            local T = (HealthPercent - 0.5) * 2
            return Color3.new(1 - T * 0.4, 1, 0)
        else
            local T = HealthPercent * 2
            return Color3.new(1, T, 0)
        end
    end

    local function DrawCornerBox(Entry, X, Y, W, H, Color)
        local CornerLen = math.min(W, H) * 0.25
        local Lines = Entry.CornerLines
        local Positions = {
            {Vector2.new(X, Y), Vector2.new(X + CornerLen, Y)},
            {Vector2.new(X, Y), Vector2.new(X, Y + CornerLen)},
            {Vector2.new(X + W, Y), Vector2.new(X + W - CornerLen, Y)},
            {Vector2.new(X + W, Y), Vector2.new(X + W, Y + CornerLen)},
            {Vector2.new(X, Y + H), Vector2.new(X + CornerLen, Y + H)},
            {Vector2.new(X, Y + H), Vector2.new(X, Y + H - CornerLen)},
            {Vector2.new(X + W, Y + H), Vector2.new(X + W - CornerLen, Y + H)},
            {Vector2.new(X + W, Y + H), Vector2.new(X + W, Y + H - CornerLen)},
        }
        for i = 1, 8 do
            Lines[i].Visible = true
            Lines[i].From = Positions[i][1]
            Lines[i].To = Positions[i][2]
            Lines[i].Color = Color
            Lines[i].Thickness = 2
        end
    end

    local function HideCornerBox(Entry)
        for i = 1, 8 do Entry.CornerLines[i].Visible = false end
    end

    local function EnsureESP(Player)
        local Entry = ESPCache[Player]
        if (not Entry) then
            Entry = {
                Box = Drawing.new("Square"),
                Name = Drawing.new("Text"),
                Health = Drawing.new("Text"),
                HealthBar = Drawing.new("Line"),
                HealthBarBg = Drawing.new("Line"),
                Tracer = Drawing.new("Line"),
                CornerLines = {},
            }
            for i = 1, 8 do
                Entry.CornerLines[i] = Drawing.new("Line")
                Entry.CornerLines[i].Thickness = 2
                Entry.CornerLines[i].Transparency = 1
            end
            Entry.Box.Thickness = 1
            Entry.Box.Transparency = 1
            Entry.Box.Color = Color3.fromRGB(255, 60, 60)
            Entry.Name.Center = true
            Entry.Name.Outline = true
            Entry.Name.OutlineColor = Color3.new(0, 0, 0)
            Entry.Name.Size = 13
            Entry.Health.Center = true
            Entry.Health.Outline = true
            Entry.Health.OutlineColor = Color3.new(0, 0, 0)
            Entry.Health.Size = 12
            Entry.HealthBar.Thickness = 3
            Entry.HealthBar.Transparency = 1
            Entry.HealthBarBg.Thickness = 3
            Entry.HealthBarBg.Transparency = 0.5
            Entry.HealthBarBg.Color = Color3.new(0, 0, 0)
            Entry.Tracer.Thickness = 1
            Entry.Tracer.Transparency = 0.7
            ESPCache[Player] = Entry
        end
        if (not SmoothCache[Player]) then
            SmoothCache[Player] = {X = 0, Y = 0, W = 0, H = 0}
        end
        if (ESPChamsToggle.Value) then
            if (not ChamCache[Player]) then
                local Highlight = Instance.new("Highlight")
                Highlight.FillTransparency = 0.6
                Highlight.OutlineTransparency = 0.2
                Highlight.Parent = Workspace.Terrain
                ChamCache[Player] = Highlight
            end
        end
        return Entry
    end

    local function CleanupPlayer(Player)
        local Entry = ESPCache[Player]
        if (Entry) then
            Entry.Box:Remove()
            Entry.Name:Remove()
            Entry.Health:Remove()
            Entry.HealthBar:Remove()
            Entry.HealthBarBg:Remove()
            Entry.Tracer:Remove()
            for i = 1, 8 do Entry.CornerLines[i]:Remove() end
            ESPCache[Player] = nil
        end
        SmoothCache[Player] = nil
        local Cham = ChamCache[Player]
        if (Cham) then Cham:Destroy() ChamCache[Player] = nil end
    end

    Players.PlayerRemoving:Connect(CleanupPlayer)

    RunService:BindToRenderStep("BlockSpinESP", Enum.RenderPriority.Character.Value, function()
        local Enabled = ESPToggle.Value
        local MaxDist = ESPDistanceSlider.Value
        local VpCenter = Camera.ViewportSize / 2
        local SmoothFactor = 0.35

        for _, Player in ipairs(Players:GetPlayers()) do
            if (Player == LocalPlayer) then
                local SelfEntry = ESPCache[Player]
                if (SelfEntry) then
                    SelfEntry.Box.Visible = false
                    SelfEntry.Name.Visible = false
                    SelfEntry.Health.Visible = false
                    SelfEntry.HealthBar.Visible = false
                    SelfEntry.HealthBarBg.Visible = false
                    SelfEntry.Tracer.Visible = false
                    HideCornerBox(SelfEntry)
                end
                continue
            end

            local Entry = EnsureESP(Player)
            local Character = Player.Character
            local RootPart = Character and Character:FindFirstChild("HumanoidRootPart")
            local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
            local Show = false

            if (Enabled and RootPart and Humanoid and Humanoid.Health > 0) then
                local IsTeammate = LocalPlayer.Team and Player.Team and Player.Team == LocalPlayer.Team
                if (not (ESPTeamCheckToggle.Value and IsTeammate)) then
                    local Range = (RootPart.Position - Camera.CFrame.Position).Magnitude
                    if (Range <= MaxDist) then
                        local Head = Character:FindFirstChild("Head")
                        local HeadTop = Head and (Head.Position + Vector3.new(0, Head.Size.Y / 2, 0)) or (RootPart.Position + Vector3.new(0, 3, 0))
                        local FeetPos = RootPart.Position - Vector3.new(0, 3, 0)
                        local ScreenTop, OnTop = Camera:WorldToScreenPoint(HeadTop)
                        local ScreenFeet, OnFeet = Camera:WorldToScreenPoint(FeetPos)

                        if (OnTop and OnFeet) then
                            local TargetTopY = math.min(ScreenTop.Y, ScreenFeet.Y)
                            local TargetBottomY = math.max(ScreenTop.Y, ScreenFeet.Y)
                            local TargetBoxH = TargetBottomY - TargetTopY
                            local TargetBoxW = TargetBoxH * 0.6
                            local TargetCenterX = (ScreenTop.X + ScreenFeet.X) / 2

                            local Smooth = SmoothCache[Player]
                            Smooth.X = Smooth.X + (TargetCenterX - TargetBoxW / 2 - Smooth.X) * SmoothFactor
                            Smooth.Y = Smooth.Y + (TargetTopY - Smooth.Y) * SmoothFactor
                            Smooth.W = Smooth.W + (TargetBoxW - Smooth.W) * SmoothFactor
                            Smooth.H = Smooth.H + (TargetBoxH - Smooth.H) * SmoothFactor

                            local X, Y, W, H = Smooth.X, Smooth.Y, Smooth.W, Smooth.H
                            local Cx = X + W / 2
                            local Color = IsTeammate and Color3.fromRGB(60, 255, 60) or ESPColorPicker.Value

                            if (ESPBoxesToggle.Value) then
                                if (ESPBoxStyleDropdown.Value == "Corner Box") then
                                    Entry.Box.Visible = false
                                    DrawCornerBox(Entry, X, Y, W, H, Color)
                                else
                                    HideCornerBox(Entry)
                                    Entry.Box.Visible = true
                                    Entry.Box.Color = Color
                                    Entry.Box.Position = Vector2.new(X, Y)
                                    Entry.Box.Size = Vector2.new(W, H)
                                end
                            else
                                Entry.Box.Visible = false
                                HideCornerBox(Entry)
                            end

                            if (ESPNameToggle.Value) then
                                Entry.Name.Visible = true
                                Entry.Name.Text = Player.Name
                                Entry.Name.Color = Color
                                Entry.Name.Position = Vector2.new(Cx, Y - 15)
                            else
                                Entry.Name.Visible = false
                            end

                            if (ESPHealthBar.Value) then
                                local HealthPct = math.clamp(Humanoid.Health / Humanoid.MaxHealth, 0, 1)
                                local BarH = H * HealthPct
                                local BarX = X - 4
                                Entry.HealthBarBg.Visible = true
                                Entry.HealthBarBg.From = Vector2.new(BarX, Y)
                                Entry.HealthBarBg.To = Vector2.new(BarX, Y + H)
                                Entry.HealthBar.Visible = true
                                Entry.HealthBar.From = Vector2.new(BarX, Y + H - BarH)
                                Entry.HealthBar.To = Vector2.new(BarX, Y + H)
                                Entry.HealthBar.Color = GetHealthColor(HealthPct)
                            else
                                Entry.HealthBar.Visible = false
                                Entry.HealthBarBg.Visible = false
                            end

                            if (ESPHealthTextToggle.Value) then
                                Entry.Health.Visible = true
                                Entry.Health.Text = string.format("%d/%d", math.floor(Humanoid.Health), math.floor(Humanoid.MaxHealth))
                                Entry.Health.Position = Vector2.new(Cx, Y + H + 2)
                            else
                                Entry.Health.Visible = false
                            end

                            if (ESPTracerToggle.Value) then
                                Entry.Tracer.Visible = true
                                Entry.Tracer.From = Vector2.new(VpCenter.X, Camera.ViewportSize.Y)
                                Entry.Tracer.To = Vector2.new(Cx, Y + H)
                                Entry.Tracer.Color = Color
                            else
                                Entry.Tracer.Visible = false
                            end

                            Show = true
                        end
                    end
                end
            end

            if (ESPChamsToggle.Value and Player.Character and Show) then
                local Cham = ChamCache[Player]
                if (Cham) then
                    Cham.FillColor = ESPChamsColorPicker.Value
                    Cham.OutlineColor = ESPChamsColorPicker.Value
                    Cham.Adornee = Player.Character
                    Cham.Enabled = true
                end
            else
                local Cham = ChamCache[Player]
                if (Cham) then Cham.Enabled = false Cham.Adornee = nil end
            end

            if (not Show) then
                Entry.Box.Visible = false
                Entry.Name.Visible = false
                Entry.Health.Visible = false
                Entry.HealthBar.Visible = false
                Entry.HealthBarBg.Visible = false
                Entry.Tracer.Visible = false
                HideCornerBox(Entry)
            end
        end
    end)
    local ESPExtrasGroup = ESPTab:AddRightGroupbox("Extras")

    local CarPVPToggle = ESPExtrasGroup:AddToggle("CarPVP", {
        Text = "Car PVP", Default = false,
    })

    local CarPVPConnection = nil

    local function StartCarPVP()
        local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
        local Humanoid = Character:WaitForChild("Humanoid")
        Camera.CameraType = Enum.CameraType.Custom
        Camera.CameraSubject = Humanoid
        if (CarPVPConnection) then CarPVPConnection:Disconnect() end
        CarPVPConnection = RunService.RenderStepped:Connect(function()
            if (not CarPVPToggle.Value) then return end
            local Char = LocalPlayer.Character
            local Hum = Char and Char:FindFirstChildOfClass("Humanoid")
            if (Hum) then
                Camera.CameraType = Enum.CameraType.Custom
                Camera.CameraSubject = Hum
            end
        end)
    end

    local function StopCarPVP()
        if (CarPVPConnection) then CarPVPConnection:Disconnect() CarPVPConnection = nil end
        local Character = LocalPlayer.Character
        local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
        if (Humanoid) then
            Camera.CameraType = Enum.CameraType.Custom
            Camera.CameraSubject = Humanoid
        end
    end

    CarPVPToggle:OnChanged(function(Value)
        if (Value) then StartCarPVP() else StopCarPVP() end
    end)

    local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

    local HudGui = Instance.new("ScreenGui")
    HudGui.Name = "InventoryHudGui"
    HudGui.ResetOnSpawn = false
    HudGui.Parent = PlayerGui

    local HudFrame = Instance.new("Frame")
    HudFrame.Name = "HudFrame"
    HudFrame.Size = UDim2.new(0, 220, 0, 300)
    HudFrame.Position = UDim2.new(0, 15, 0.3, 0)
    HudFrame.BackgroundTransparency = 1
    HudFrame.BorderSizePixel = 0
    HudFrame.Visible = false
    HudFrame.Parent = HudGui

    local HudTitle = Instance.new("TextLabel")
    HudTitle.Size = UDim2.new(1, 0, 0, 30)
    HudTitle.BackgroundTransparency = 1
    HudTitle.Text = "Inventory Checker"
    HudTitle.TextColor3 = Color3.fromRGB(228, 228, 231)
    HudTitle.TextSize = 14
    HudTitle.Font = Enum.Font.SourceSansBold
    HudTitle.TextXAlignment = Enum.TextXAlignment.Left
    HudTitle.Parent = HudFrame

    local HudScroll = Instance.new("ScrollingFrame")
    HudScroll.Size = UDim2.new(1, 0, 1, -30)
    HudScroll.Position = UDim2.new(0, 0, 0, 30)
    HudScroll.BackgroundTransparency = 1
    HudScroll.BorderSizePixel = 0
    HudScroll.ScrollBarThickness = 2
    HudScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    HudScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
    HudScroll.Parent = HudFrame

    local HudTextLabel = Instance.new("TextLabel")
    HudTextLabel.Size = UDim2.new(1, 0, 0, 0)
    HudTextLabel.AutomaticSize = Enum.AutomaticSize.Y
    HudTextLabel.BackgroundTransparency = 1
    HudTextLabel.Text = "Scanning inventories..."
    HudTextLabel.TextColor3 = Color3.fromRGB(212, 212, 216)
    HudTextLabel.TextSize = 13
    HudTextLabel.Font = Enum.Font.SourceSansBold
    HudTextLabel.TextXAlignment = Enum.TextXAlignment.Left
    HudTextLabel.TextYAlignment = Enum.TextYAlignment.Top
    HudTextLabel.TextWrapped = true
    HudTextLabel.Parent = HudScroll

    local InvCheckerToggle = ESPExtrasGroup:AddToggle("InventoryChecker", {
        Text = "Inventory Checker HUD",
        Default = false,
        Callback = function(Value) HudFrame.Visible = Value end,
    })

    local ITEM_DATABASE = {
        {meshPattern = "c9_cube", displayName = "C9", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "glock_colt45", displayName = "Glock", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "uzi_cube", displayName = "UZI", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "m24_cube", displayName = "M24", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "revolver anaconda_cube", displayName = "Anaconda", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "mp5_cube", displayName = "MP5", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "firework launcher_cylinder", displayName = "FireworkL", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "remington_cube", displayName = "RemingtonSG", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "double barrel shotgun_cube", displayName = "Double/Sawnoff", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "rpg_cube", displayName = "RPG", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "p226_cube", displayName = "P226", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "huntingrifle_cube", displayName = "HuntingR", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "g3_cube", displayName = "G3", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "draco_cube", displayName = "Draco", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "ak47_cube", displayName = "AK47", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "crossbox_cube", displayName = "Crossbow", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "m16 aug_Cube", displayName = "Skorpion", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "aug_Cube", displayName = "M16", color = Color3.new(1, 1, 1), isPrefix = true},
        {meshPattern = "new melees_cylinder.002", displayName = "Sledge Hammer", color = Color3.new(1, 165/255, 0)},
        {meshPattern = "weapon tools_cylinder.005", displayName = "Shovel", color = Color3.new(1, 165/255, 0)},
        {meshPattern = "weapon tools_cube.005", displayName = "Machete", color = Color3.new(1, 165/255, 0)},
        {meshPattern = "weapon tools_cylinder", displayName = "Crowbar", color = Color3.new(1, 165/255, 0)},
        {meshPattern = "new melees_plane.003", displayName = "Butcher Knife", color = Color3.new(1, 165/255, 0)},
        {meshPattern = "weapon tools_cube.001", displayName = "Wrench", color = Color3.new(1, 165/255, 0)},
        {meshPattern = "new melees_cylinder.004", displayName = "Tactical Shovel", color = Color3.new(1, 1, 0)},
        {meshPattern = "new melees_plane.005", displayName = "Tactical Knife", color = Color3.new(1, 1, 0)},
        {meshPattern = "new melees_plane", displayName = "Tactical Axe", color = Color3.new(1, 1, 0)},
        {meshPattern = "weapon tools_cube.006", displayName = "Switchblade", color = Color3.new(1, 165/255, 0)},
        {meshPattern = "weapon tools_1.001", displayName = "Barbed Bat", color = Color3.new(1, 0, 0)},
        {meshPattern = "1a_Plane", displayName = "Combat Axe", color = Color3.new(1, 0, 0), isPrefix = true}
    }

    task.spawn(function()
        while true do
            pcall(function()
                if HudFrame.Visible then
                    local DISPLAYENTRIES = {}
                    for _, instance in ipairs(Players:GetChildren()) do
                        if not instance:IsA("Player") then continue end
                        local pObj = instance
                        local pName = pObj.Name
                        local SETITEMS = {}
                        local ITEMTODISPLAY = {}
                        local ITEMCHECK = {}

                        local backpack = pObj:FindFirstChild("Backpack")
                        if backpack then
                            for _, tool in ipairs(backpack:GetChildren()) do
                                if tool:IsA("Tool") then table.insert(ITEMCHECK, tool) end
                            end
                        end
                        local character = pObj.Character
                        if character then
                            for _, child in ipairs(character:GetChildren()) do
                                if child:IsA("Tool") then table.insert(ITEMCHECK, child) end
                            end
                        end

                        for _, tool in ipairs(ITEMCHECK) do
                            local identified = false
                            for _, part in ipairs(tool:GetChildren()) do
                                local partName = part.Name
                                local prefix = "Meshes/"
                                if string.sub(partName, 1, #prefix) == prefix then
                                    local subName = string.lower(string.sub(partName, #prefix + 1))
                                    for _, entry in ipairs(ITEM_DATABASE) do
                                        local pattern = string.lower(entry.meshPattern)
                                        local matched = false
                                        if entry.isPrefix then
                                            if string.sub(subName, 1, #pattern) == pattern then matched = true end
                                        else
                                            if subName == pattern then matched = true end
                                        end
                                        if matched then
                                            if not SETITEMS[entry.displayName] then
                                                SETITEMS[entry.displayName] = true
                                                table.insert(ITEMTODISPLAY, "- " .. entry.displayName)
                                            end
                                            identified = true
                                            break
                                        end
                                    end
                                elseif partName == "Butt" then
                                    if not SETITEMS["Axe"] then
                                        SETITEMS["Axe"] = true
                                        table.insert(ITEMTODISPLAY, "- Axe")
                                    end
                                    identified = true
                                elseif partName == "Bat" then
                                    if not SETITEMS["Bat"] then
                                        SETITEMS["Bat"] = true
                                        table.insert(ITEMTODISPLAY, "- Bat")
                                    end
                                    identified = true
                                end
                                if identified then break end
                            end
                        end

                        if #ITEMTODISPLAY > 0 then
                            table.insert(DISPLAYENTRIES, pName .. ":\n" .. table.concat(ITEMTODISPLAY, "\n"))
                        end
                    end

                    local resultStr = table.concat(DISPLAYENTRIES, "\n\n")
                    if resultStr == "" then
                        HudTextLabel.Text = "No player inventories detected."
                    else
                        HudTextLabel.Text = resultStr
                    end
                end
            end)
            task.wait(0.5)
        end
    end)

    local UISettingsGroup = Tabs["UI Settings"]:AddLeftGroupbox("UI")

    local ShowUIToggle = UISettingsGroup:AddToggle("ShowUI", {
        Text = "Show UI",
        Default = true,
        Callback = function(Value) Window.Holder.Visible = Value end,
    })
    ShowUIToggle:AddKeyPicker("ShowUIKey", {
        Text = "UI Toggle Key",
        Default = Enum.KeyCode.RightShift,
        SyncToggleState = true,
        Mode = "Toggle",
        Modes = {"Toggle"},
        IgnoreProcessedCheck = true,
    })

    local PanicKeyGroup = Tabs["UI Settings"]:AddLeftGroupbox("Safety")

    local PanicKeyBind = PanicKeyGroup:AddKeyPicker("PanicKeyBind", {
        Text = "Panic Key",
        Default = Enum.KeyCode.End,
        Mode = "Toggle",
        Modes = {"Toggle"},
        IgnoreProcessedCheck = true,
    })

    PanicKeyGroup:AddButton({
        Text = "PANIC - Disable All",
        Func = function()
            EnabledToggle:SetValue(false)
            ESPToggle:SetValue(false)
            SilentAimToggle:SetValue(false)
            CarPVPToggle:SetValue(false)
            InvCheckerToggle:SetValue(false)
            ESPChamsToggle:SetValue(false)
            Library:Notify("PANIC: All features disabled", 3)
        end,
    })

    task.spawn(function()
        while task.wait(0.1) do
            if (PanicKeyBind:GetState()) then
                EnabledToggle:SetValue(false)
                ESPToggle:SetValue(false)
                SilentAimToggle:SetValue(false)
                CarPVPToggle:SetValue(false)
                InvCheckerToggle:SetValue(false)
                ESPChamsToggle:SetValue(false)
            end
        end
    end)
    local ThemeGroup = Tabs["UI Settings"]:AddRightGroupbox("Theme")
    local ThemeColorPickers = {}

    local BGColorPicker = ThemeGroup:AddColorPicker("BackgroundColor", {
        Text = "Background Color", Default = Library.BackgroundColor,
    })
    ThemeColorPickers.BackgroundColor = BGColorPicker

    local MainColorPicker = ThemeGroup:AddColorPicker("MainColor", {
        Text = "Main Color", Default = Library.MainColor,
    })
    ThemeColorPickers.MainColor = MainColorPicker

    local AccentColorPicker = ThemeGroup:AddColorPicker("AccentColor", {
        Text = "Accent Color", Default = Library.AccentColor,
    })
    ThemeColorPickers.AccentColor = AccentColorPicker

    local OutlineColorPicker = ThemeGroup:AddColorPicker("OutlineColor", {
        Text = "Outline Color", Default = Library.OutlineColor,
    })
    ThemeColorPickers.OutlineColor = OutlineColorPicker

    local FontColorPicker = ThemeGroup:AddColorPicker("FontColor", {
        Text = "Font Color", Default = Library.FontColor,
    })
    ThemeColorPickers.FontColor = FontColorPicker

    local function ApplyTheme()
        Library.BackgroundColor = ThemeColorPickers.BackgroundColor.Value
        Library.MainColor = ThemeColorPickers.MainColor.Value
        Library.AccentColor = ThemeColorPickers.AccentColor.Value
        Library.OutlineColor = ThemeColorPickers.OutlineColor.Value
        Library.FontColor = ThemeColorPickers.FontColor.Value
        Library.AccentColorDark = Library:GetDarkerColor(Library.AccentColor)
        Library:UpdateColorsUsingRegistry()
    end

    BGColorPicker:OnChanged(function() ApplyTheme() end)
    MainColorPicker:OnChanged(function() ApplyTheme() end)
    AccentColorPicker:OnChanged(function() ApplyTheme() end)
    OutlineColorPicker:OnChanged(function() ApplyTheme() end)
    FontColorPicker:OnChanged(function() ApplyTheme() end)

    local HttpService = game:GetService("HttpService")
    local CONFIG_FOLDER = "BlockSpinLegit/Configs/settings"

    for _, Folder in ipairs({ "BlockSpinLegit", "BlockSpinLegit/Configs", CONFIG_FOLDER }) do
        if not isfolder(Folder) then makefolder(Folder) end
    end

    local SaveToggles = {
        Enabled = EnabledToggle,
        TeamCheck = TeamCheckToggle,
        WallCheck = WallCheckToggle,
        SilentAim = SilentAimToggle,
        ESPEnabled = ESPToggle,
        ESPBoxes = ESPBoxesToggle,
        ESPName = ESPNameToggle,
        ESPHealth = ESPHealthToggle,
        ESPHealthText = ESPHealthTextToggle,
        ESPTracers = ESPTracerToggle,
        ESPTeamCheck = ESPTeamCheckToggle,
        ESPChams = ESPChamsToggle,
        CarPVP = CarPVPToggle,
        InventoryChecker = InvCheckerToggle,
    }

    local SaveOptions = {
        EnabledKey = EnabledKey,
        FOV = FOVSlider,
        Hitbox = HitboxDropdown,
        Smoothness = SmoothnessSlider,
        MaxDistance = MaxDistanceSlider,
        Prediction = PredictionSlider,
        Priority = PriorityDropdown,
        ESPBoxStyle = ESPBoxStyleDropdown,
        ESPDistance = ESPDistanceSlider,
        ESPColor = ESPColorPicker,
        ESPChamsColor = ESPChamsColorPicker,
        BackgroundColor = BGColorPicker,
        MainColor = MainColorPicker,
        AccentColor = AccentColorPicker,
        OutlineColor = OutlineColorPicker,
        FontColor = FontColorPicker,
    }

    local ConfigGroup = Tabs["UI Settings"]:AddRightGroupbox("Configuration")

    local ConfigListDropdown = ConfigGroup:AddDropdown("ConfigList", {
        Text = "Config list", Values = {}, AllowNull = true,
    })

    local ConfigNameInput = ConfigGroup:AddInput("ConfigName", {
        Text = "Config name",
    })

    local function GetConfigList()
        local out = {}
        for _, file in ipairs(listfiles(CONFIG_FOLDER)) do
            local Name = file:match("([^/\\]+)%.json$")
            if (Name) then table.insert(out, Name) end
        end
        return out
    end

    local function RefreshConfigList()
        ConfigListDropdown.Values = GetConfigList()
        ConfigListDropdown:SetValues()
        ConfigListDropdown:SetValue(nil)
    end

    local function SaveConfig(Name)
        if (not Name or Name:gsub("%s", "") == "") then
            return Library:Notify("Invalid config name (empty)", 2)
        end
        local Data = { objects = {} }
        for Idx, Toggle in next, SaveToggles do
            table.insert(Data.objects, { type = "Toggle", idx = Idx, value = Toggle.Value })
        end
        for Idx, Option in next, SaveOptions do
            local Type = Option.Type
            if (Type == "Slider") then
                table.insert(Data.objects, { type = "Slider", idx = Idx, value = tostring(Option.Value) })
            elseif (Type == "Dropdown") then
                table.insert(Data.objects, { type = "Dropdown", idx = Idx, value = Option.Value, mutli = Option.Multi })
            elseif (Type == "KeyPicker") then
                table.insert(Data.objects, { type = "KeyPicker", idx = Idx, mode = Option.Mode, key = Option.Value })
            elseif (Type == "ColorPicker") then
                table.insert(Data.objects, { type = "ColorPicker", idx = Idx, r = Option.Value.R, g = Option.Value.G, b = Option.Value.B })
            end
        end
        local Success, Encoded = pcall(HttpService.JSONEncode, HttpService, Data)
        if (not Success) then
            return Library:Notify("Failed to save config: encode error", 3)
        end
        writefile(CONFIG_FOLDER .. "/" .. Name .. ".json", Encoded)
        Library:Notify(string.format('Created config "%s"', Name), 3)
        RefreshConfigList()
    end

    local function LoadConfig(Name)
        if (not Name) then return Library:Notify("No config selected", 2) end
        local Path = CONFIG_FOLDER .. "/" .. Name .. ".json"
        if (not isfile(Path)) then return Library:Notify("Config not found", 2) end
        local Success, Decoded = pcall(HttpService.JSONDecode, HttpService, readfile(Path))
        if (not Success) then return Library:Notify("Failed to load config: decode error", 3) end
        for _, Object in next, Decoded.objects do
            local Toggle = SaveToggles[Object.idx]
            local Option = SaveOptions[Object.idx]
            if (Toggle and Object.type == "Toggle") then
                Toggle:SetValue(Object.value)
            elseif (Option) then
                if (Object.type == "Slider") then
                    Option:SetValue(Object.value)
                elseif (Object.type == "Dropdown") then
                    Option:SetValue(Object.value)
                elseif (Object.type == "KeyPicker") then
                    Option:SetValue({ Object.key, Object.mode })
                elseif (Object.type == "ColorPicker") then
                    Option:SetValue(Color3.new(Object.r, Object.g, Object.b))
                end
            end
        end
        ApplyTheme()
        Library:Notify(string.format('Loaded config "%s"', Name), 3)
    end

    ConfigGroup:AddDivider()
    ConfigGroup:AddButton({ Text = "Create config", Func = function() SaveConfig(ConfigNameInput.Value) end })
    ConfigGroup:AddButton({ Text = "Overwrite config", Func = function()
        local Name = ConfigListDropdown.Value
        if (Name) then SaveConfig(Name) else Library:Notify("No config selected", 2) end
    end })
    ConfigGroup:AddButton({ Text = "Load config", Func = function() LoadConfig(ConfigListDropdown.Value) end })
    ConfigGroup:AddButton({ Text = "Refresh config list", Func = RefreshConfigList })

    local AutoloadLabel = ConfigGroup:AddLabel("Current autoload config: none", true)
    local AUTOLOAD_FILE = CONFIG_FOLDER .. "/autoload.txt"

    ConfigGroup:AddButton({ Text = "Autoload config", Func = function()
        local Name = ConfigListDropdown.Value
        if (not Name) then return Library:Notify("No config selected", 2) end
        writefile(AUTOLOAD_FILE, Name)
        AutoloadLabel:SetText("Current autoload config: " .. Name)
        Library:Notify(string.format('Set "%s" to auto load', Name), 3)
    end })

    if (isfile(AUTOLOAD_FILE)) then
        local Name = readfile(AUTOLOAD_FILE)
        AutoloadLabel:SetText("Current autoload config: " .. Name)
        LoadConfig(Name)
    end

    Window:SelectTab(1)
else
    print("[ERROR]: Invalid or missing script key.")
    print("  Set _G.script_key = \"<key>\" BEFORE running this script.")
    print("  Valid keys: " .. GetValidKeyList())
    print('  You entered: "' .. tostring(userEnteredKey) .. '"')
    ShowKeyError()
end
